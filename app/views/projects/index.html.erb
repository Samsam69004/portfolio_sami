<% content_for :title, "Développeur web junior à Lyon — Portfolio Sami Chabane" %>
<% content_for :meta_description, "Sites vitrines et petites apps modernes en Ruby on Rails, HTML/CSS/Tailwind. Focus performance, sécurité et maintenabilité." %>

<% content_for :og_title, "Portfolio — Sami Chabane" %>
<% content_for :og_description, "Développeur web junior à Lyon. Découvrez mes projets Ruby on Rails, JavaScript, Tailwind." %>

<section id="home" class="relative overflow-hidden">
  <div class="absolute inset-0 -z-10 bg-gradient-to-b from-slate-50 to-white"></div>
  <canvas id="stars" class="absolute inset-0 -z-10"></canvas>

  <div class="max-w-6xl mx-auto px-4 sm:px-6 py-20 md:py-28 grid md:grid-cols-12 gap-10 items-center">
    <!-- Texte -->
    <div class="md:col-span-7">
      <p data-aos="fade-up" class="text-sm font-medium tracking-wide text-sky-600 mb-3">
        Développeur Web Junior — Ruby on Rails & Front-end
      </p>

      <h1 id="hero-title" data-aos="fade-up" data-aos-delay="100" class="text-4xl md:text-6xl font-extrabold leading-tight text-slate-900">
        Des sites <span class="text-sky-500">modernes, sécurisés, maintenables</span>
      </h1>

      <p data-aos="fade-up" data-aos-delay="180" class="mt-5 text-lg text-slate-600 max-w-prose">
        Je conçois et développe des <strong>sites vitrines</strong> et <strong>applications web</strong> avec
        <strong>Ruby on Rails</strong>, en mettant l’accent sur la <strong>performance</strong>, la
        <strong>sécurité</strong> (CSRF, XSS, SQL injection) et la <strong>maintenabilité</strong> du code.
        Mon approche : livrer des solutions <strong>scalables</strong>, pensées pour évoluer et s’adapter aux besoins réels.
        En parallèle, je renforce mes compétences en <strong>Java & Angular</strong> pour élargir ma stack.
      </p>

      <ul class="mt-6 grid sm:grid-cols-2 gap-2 text-slate-700">
        <li data-aos="fade-up" data-aos-delay="220" class="flex items-center gap-2">
          <span class="w-1.5 h-1.5 rounded-full bg-sky-500"></span> Architecture MVC (Rails)
        </li>
        <li data-aos="fade-up" data-aos-delay="260" class="flex items-center gap-2">
          <span class="w-1.5 h-1.5 rounded-full bg-sky-500"></span> Sécurité intégrée (CSRF, XSS, SQLi)
        </li>
        <li data-aos="fade-up" data-aos-delay="300" class="flex items-center gap-2">
          <span class="w-1.5 h-1.5 rounded-full bg-sky-500"></span> Responsive design & accessibilité
        </li>
        <li data-aos="fade-up" data-aos-delay="340" class="flex items-center gap-2">
          <span class="w-1.5 h-1.5 rounded-full bg-sky-500"></span> Optimisation SEO on-page avancée
        </li>
      </ul>

      <div data-aos="fade-up" data-aos-delay="400" class="mt-8 flex flex-wrap gap-3">
        <a href="#projects" class="rounded-xl bg-sky-500 px-5 py-3 text-white shadow hover:bg-sky-600 transition">Voir mes projets</a>
        <a href="#contact" class="rounded-xl border border-slate-300 px-5 py-3 hover:border-sky-600 hover:text-sky-600 transition">Me contacter</a>
        <a href="/CV_Sami_Chabane.pdf" target="_blank" rel="noopener noreferrer" class="rounded-xl border border-slate-300 px-5 py-3 hover:border-sky-600 hover:text-sky-600 transition">📄 Télécharger mon CV</a>
      </div>

      <ul data-aos="fade-up" data-aos-delay="460" class="mt-8 flex flex-wrap gap-3 text-sm text-slate-600">
        <li class="px-3 py-1 rounded-full bg-white border">Ruby on Rails</li>
        <li class="px-3 py-1 rounded-full bg-white border">MVC</li>
        <li class="px-3 py-1 rounded-full bg-white border">ERB</li>
        <li class="px-3 py-1 rounded-full bg-white border">HTML5</li>
        <li class="px-3 py-1 rounded-full bg-white border">CSS3 / Tailwind</li>
        <li class="px-3 py-1 rounded-full bg-white border">JavaScript ES6</li>
        <li class="px-3 py-1 rounded-full bg-white border">Java (en cours)</li>
        <li class="px-3 py-1 rounded-full bg-white border">Angular (en cours)</li>
        <li class="px-3 py-1 rounded-full bg-white border">SEO on-page</li>
        <li class="px-3 py-1 rounded-full bg-white border">Community Management</li>
      </ul>

      <a href="#projects" class="mt-10 inline-block animate-bounce text-slate-400" aria-label="Faire défiler">⌄</a>
    </div>

    <!-- Visuel -->
    <div data-aos="zoom-in" data-aos-delay="120" class="md:col-span-5 md:justify-self-end">
      <div class="relative rounded-2xl overflow-hidden shadow-xl ring-1 ring-slate-200 bg-white">
        <img
          src="https://images.unsplash.com/photo-1498050108023-c5249f4df085?q=80&w=1200&auto=format&fit=crop"
          alt="Poste de travail développement web"
          class="w-full h-80 object-cover">
      </div>
      <p class="mt-3 text-xs text-slate-500 text-center">Code propre, sécurisé et pensé pour durer.</p>
    </div>
  </div>
</section>

<!-- SECTION: Stack & outils -->
<section id="stack" class="scroll-mt-24 py-20 bg-slate-50">
  <div class="mx-auto max-w-6xl px-6">
    <header class="text-left md:text-center mb-12" data-aos="fade-up">
      <h2 class="text-3xl md:text-4xl font-bold text-sky-500">
        Stack & outils
      </h2>
      <p class="mt-3 text-slate-600">
        Les technos que j’utilise pour livrer des sites
        <span class="text-sky-600 font-medium">modernes, sécurisés, maintenables</span>.
      </p>
    </header>

    <% stack = [
      { name: "Ruby on Rails",  icon: :rails,  note: "MVC, sécurité, ActiveAdmin" },
      { name: "JavaScript (ES6)", icon: :js,   note: "Interactions front" },
      { name: "HTML5",          icon: :html,  note: "Sémantique & accessibilité" },
      { name: "CSS3 / Tailwind",icon: :css,   note: "UI propre, responsive" },
      { name: "Git & GitHub",   icon: :git,   note: "Versionning & workflow" },
      { name: "REST API",       icon: :api,   note: "Intégrations & services" }
    ] %>

    <ul class="grid gap-6 sm:grid-cols-2 md:grid-cols-3">
      <% stack.each_with_index do |t, i| %>
        <li class="group rounded-2xl border border-slate-200 bg-white p-6 shadow-sm hover:shadow-lg transition hover:-translate-y-0.5"
            data-aos="fade-up" data-aos-delay="<%= i*80 %>">
          <div class="flex items-start gap-4">
            <div class="grid place-items-center h-14 w-14 rounded-xl bg-sky-50 ring-1 ring-sky-100">
              <% case t[:icon]
                when :rails %>
                  <%= image_tag "stack/rails.png", alt: "Logo Ruby on Rails", class: "h-8 w-8" %>
                <% when :js %>
                  <svg viewBox="0 0 448 512" class="h-8 w-8 text-yellow-500 fill-current"><path d="M0 32v448h448V32H0zm244 350c0 43-26 63-63 63-33 0-53-17-63-38l34-21c7 12 13 22 27 22 14 0 23-5 23-26V224h42v158z"/></svg>
                <% when :html %>
                  <svg viewBox="0 0 384 512" class="h-8 w-8 text-orange-500 fill-current"><path d="M0 32l34 384 158 44 158-44 34-384H0zm308 127H124l5 57h173l-11 121-83 23-83-23-6-65h41l3 34 45 12 45-12 5-55H83L73 96h238l-3 63z"/></svg>
                <% when :css %>
                  <svg viewBox="0 0 384 512" class="h-8 w-8 text-sky-600 fill-current"><path d="M0 32l34 384 158 44 158-44 34-384H0zm308 96l-3 32H126l5 57h170l-12 131-95 26-95-26-6-65h41l3 34 57 15 57-15 6-68H118l-10-121h200z"/></svg>
                <% when :git %>
                  <svg viewBox="0 0 256 256" class="h-8 w-8 text-rose-500 fill-current"><path d="M251.6 121.1L134.9 4.4c-5.9-5.9-15.4-5.9-21.3 0L92.3 25.7l27.1 27.1c6-2 12.9-.7 17.7 4.1 4.8 4.8 6.2 11.7 4.1 17.7l26.1 26.1c6-2 12.9-.7 17.7 4.1 7 7 7 18.3 0 25.3-7 7-18.3 7-25.3 0-4.9-4.9-6.3-12-4-18l-24.2-24.2v63.6a18 18 0 1012 16.9V135l24.2 24.2a18 18 0 1025.3-25.3c-4.9-4.9-12-6.3-18-4L170 103c2.2-6 .8-12.8-4.1-17.7-4.8-4.8-11.7-6.1-17.7-4.1L92.3 25.7 4.4 113.6c-5.9 5.9-5.9 15.4 0 21.3l116.7 116.7c5.9 5.9 15.4 5.9 21.3 0l109.2-109.2c5.9-5.8 5.9-15.4 0-21.3z"/></svg>
                <% when :api %>
                  <svg viewBox="0 0 512 512" class="h-8 w-8 text-sky-600 fill-current"><path d="M256 112a56 56 0 100 112 56 56 0 000-112zM32 240h96v32H32zm352 0h96v32h-96zM256 368a56 56 0 100-112 56 56 0 000 112z"/></svg>
              <% end %>
            </div>
            <div class="pt-1">
              <p class="text-base font-semibold text-slate-900"><%= t[:name] %></p>
              <p class="text-sm text-slate-600"><%= t[:note] %></p>
            </div>
          </div>
        </li>
      <% end %>
    </ul>

    <div class="mt-8 flex flex-wrap gap-2" data-aos="fade-up" data-aos-delay="420">
      <span class="px-3 py-1 text-sm rounded-full bg-slate-100 border border-slate-200 text-slate-700">ERB</span>
      <span class="px-3 py-1 text-sm rounded-full bg-slate-100 border border-slate-200 text-slate-700">SEO on-page</span>
      <span class="px-3 py-1 text-sm rounded-full bg-sky-50 border border-sky-200 text-sky-700">AOS</span>
      <span class="px-3 py-1 text-sm rounded-full bg-slate-100 border border-slate-200 text-slate-700">ActiveAdmin</span>
      <span class="px-3 py-1 text-sm rounded-full bg-amber-50 border border-amber-200 text-amber-700">Java (en cours)</span>
      <span class="px-3 py-1 text-sm rounded-full bg-amber-50 border border-amber-200 text-amber-700">Angular (en cours)</span>
    </div>

    <div class="mt-10 md:flex md:justify-center" data-aos="fade-up" data-aos-delay="520">
      <a href="#projects" class="inline-flex items-center gap-2 rounded-xl bg-sky-500 px-5 py-3 text-white shadow-md hover:shadow-lg hover:bg-sky-600 transition focus:outline-none focus:ring-2 focus:ring-sky-400">
        Voir mes projets
        <svg viewBox="0 0 24 24" class="h-5 w-5" fill="none" stroke="currentColor" stroke-width="2"><path d="M5 12h14m-6-6 6 6-6 6" stroke-linecap="round" stroke-linejoin="round"/></svg>
      </a>
    </div>
  </div>
</section>


<section id="projects" class="scroll-mt-24 py-16 md:py-24 bg-white">
  <div class="max-w-6xl mx-auto px-4 sm:px-6">
    <div class="mb-10 md:mb-14 flex items-end justify-between gap-4">
      <div>
        <h2 data-aos="fade-up" class="text-2xl md:text-4xl font-bold">Projets récents</h2>
        <p data-aos="fade-up" data-aos-delay="100" class="mt-3 text-slate-600">Affichés depuis la base avec Rails.</p>
      </div>
      <form data-aos="fade-left" class="flex gap-2 items-center">
        <select name="tech" class="rounded-xl border border-slate-300 px-3 py-2">
          <option value="">— Filtrer par techno —</option>
          <% ["HTML","CSS","Tailwind","JS","Rails","ERB"].each do |t| %>
            <option value="<%= t %>" <%= "selected" if params[:tech] == t %>><%= t %></option>
          <% end %>
        </select>
        <button class="rounded-xl border border-slate-300 px-3 py-2 hover:border-sky-600 hover:text-sky-600">Filtrer</button>
      </form>
    </div>

    <div class="grid sm:grid-cols-2 lg:grid-cols-2 gap-10">
      <% @projects.each_with_index do |p, i| %>
        <%= link_to project_path(p), class: "group block rounded-2xl overflow-hidden bg-white shadow-sm ring-1 ring-slate-200 transition hover:-translate-y-1 hover:shadow-xl" do %>
          <!-- Image 16:9 -->
          <div class="relative aspect-[16/9] bg-slate-100">
            <% images = p.image_url.to_s.split(",") %>
            <%= image_tag images.first.strip, alt: p.title, class: "absolute inset-0 h-full w-full object-cover" %>
            <div class="absolute inset-0 ring-0 ring-sky-500/0 group-hover:ring-2 group-hover:ring-sky-500/40 transition"></div>
          </div>

          <!-- Contenu -->
          <div class="p-5">
            <h3 class="text-2xl font-extrabold tracking-tight text-slate-900 group-hover:text-sky-600 transition"><%= p.title %></h3>
            <% if p.subtitle.present? %>
              <p class="mt-1 text-slate-600"><%= p.subtitle %></p>
            <% end %>
            <% if p.description.present? %>
              <p class="mt-3 text-slate-600"><%= truncate(p.description, length: 220) %></p>
            <% end %>

            <div class="mt-4 flex flex-wrap gap-2 text-sm">
              <% p.techs.to_s.split(",").map(&:strip).each do |t| %>
                <span class="px-3 py-1 rounded-full bg-slate-100 text-slate-700 ring-1 ring-slate-200"><%= t %></span>
              <% end %>
              <% if p.url.present? %>
                <span class="px-3 py-1 rounded-full bg-emerald-100 text-emerald-800">En ligne</span>
              <% else %>
                <span class="px-3 py-1 rounded-full bg-yellow-100 text-yellow-800">Non hébergé</span>
              <% end %>
            </div>
          </div>
        <% end %>
      <% end %>
    </div>
  </div>
</section>

<!-- Section Certifications -->
<section id="certifications" class="py-20 md:py-28 bg-white">
  <div class="max-w-6xl mx-auto px-4 sm:px-6">
    <h2 data-aos="fade-up" class="text-3xl md:text-4xl font-bold mb-6">Certifications</h2>
    <p data-aos="fade-up" data-aos-delay="100" class="text-slate-600 mb-8">Voici mes diplômes et attestations, consultables directement.</p>

    <div class="grid gap-10 md:grid-cols-2">
      <!-- Diplôme -->
      <div data-aos="fade-up" class="bg-white p-5 rounded-2xl shadow">
        <div class="flex items-center justify-between mb-3">
          <h3 class="font-semibold text-lg">Diplôme — Le Wagon</h3>
          <%= link_to "Télécharger", asset_path("diploma.pdf"), class: "text-sm px-3 py-1 rounded-full border hover:shadow", target: "_blank", rel: "noopener" %>
        </div>
        <iframe src="<%= asset_path('diploma.pdf') %>#view=FitH&zoom=125" loading="lazy" class="w-full h-[32rem] sm:h-[38rem] lg:h-[46rem] rounded-xl border" title="Diplôme Le Wagon (PDF)"></iframe>
        <p class="mt-2 text-xs text-slate-500">
          Si le PDF ne s’affiche pas,
          <%= link_to "ouvre-le dans un nouvel onglet", asset_path("diploma.pdf"), target: "_blank", rel: "noopener", class: "underline" %>.
        </p>
      </div>

      <!-- Attestation -->
      <div data-aos="fade-up" data-aos-delay="150" class="bg-white p-5 rounded-2xl shadow">
        <div class="flex items-center justify-between mb-3">
          <h3 class="font-semibold text-lg">Attestation de fin de formation</h3>
          <%= link_to "Télécharger", asset_path("certificate_of_completion.pdf"), class: "text-sm px-3 py-1 rounded-full border hover:shadow", target: "_blank", rel: "noopener" %>
        </div>
        <iframe src="<%= asset_path('certificate_of_completion.pdf') %>#view=FitH&zoom=125" loading="lazy" class="w-full h-[32rem] sm:h-[38rem] lg:h-[46rem] rounded-xl border" title="Attestation de fin de formation (PDF)"></iframe>
        <p class="mt-2 text-xs text-slate-500">
          Si le PDF ne s’affiche pas,
          <%= link_to "ouvre-le dans un nouvel onglet", asset_path("certificate_of_completion.pdf"), target: "_blank", rel: "noopener", class: "underline" %>.
        </p>
      </div>
    </div>
  </div>
</section>

<section id="passions" class="py-16 md:py-24 bg-white border-t border-slate-200">
  <div class="max-w-6xl mx-auto px-4 sm:px-6 grid md:grid-cols-2 gap-10 items-center">
    <!-- Texte -->
    <div data-aos="fade-right">
      <h2 class="text-2xl md:text-4xl font-bold text-sky-500">Mes passions</h2>
      <p class="mt-4 text-slate-700 leading-relaxed">
        Le sport et les voyages occupent une place essentielle dans ma vie, car ils forgent le caractère et développent des valeurs que je retrouve aussi dans mon parcours professionnel.
      </p>

      <ul class="mt-6 space-y-4 text-slate-700">
        <li class="flex items-center gap-3"><span class="text-2xl">⚽</span><span>Le <strong>football</strong> : école de la combativité et du goût de l’effort collectif. Je supporte l’<strong>Olympique Lyonnais</strong>.</span></li>
        <li class="flex items-center gap-3"><span class="text-2xl">🎾</span><span>Le <strong>tennis</strong> : rester concentré, gérer la pression et ne jamais lâcher.</span></li>
        <li class="flex items-center gap-3"><span class="text-2xl">🌍</span><span>Les <strong>voyages</strong> : découvrir, sortir de la zone de confort, élargir ma vision du monde.</span></li>
      </ul>
    </div>

    <!-- Carrousel -->
    <div class="md:col-span-1">
      <div class="relative h-[540px] overflow-hidden rounded-3xl shadow-xl ring-1 ring-slate-200 bg-white"
           data-carousel data-autoplay="true" data-interval="3500" data-pause-on-hover="true">
        <% slides = [ asset_path("passions/voyage.jpg"), asset_path("passions/ol.jpg"), asset_path("passions/tennis.jpg") ] %>
        <% slides.each_with_index do |src, i| %>
          <img src="<%= src %>" alt="Mes passions — slide <%= i+1 %> / <%= slides.size %>"
               class="carousel-slide <%= i.zero? ? '' : 'hidden' %> w-full h-[540px] object-cover">
        <% end %>
        <button type="button" class="absolute left-3 top-1/2 -translate-y-1/2 px-3 py-1.5 text-3xl bg-white/70 rounded-full" aria-label="Précédent" data-action="prev">‹</button>
        <button type="button" class="absolute right-3 top-1/2 -translate-y-1/2 px-3 py-1.5 text-3xl bg-white/70 rounded-full" aria-label="Suivant" data-action="next">›</button>
        <div class="absolute bottom-3 inset-x-0 flex justify-center gap-2">
          <% slides.each_with_index do |_, i| %>
            <button type="button" class="carousel-dot h-2.5 w-2.5 rounded-full <%= i.zero? ? 'bg-slate-900' : 'bg-slate-300' %>" aria-label="Aller à l’image <%= i+1 %>" data-goto="<%= i %>"></button>
          <% end %>
        </div>
      </div>
    </div>
  </div>
</section>

<section id="about" class="py-16 md:py-24 bg-white">
  <div class="max-w-6xl mx-auto px-4 sm:px-6 grid md:grid-cols-2 gap-10 items-center">
    <!-- Texte -->
    <div data-aos="fade-right">
      <h2 class="text-2xl md:text-4xl font-bold">À propos</h2>
      <p class="mt-4 text-slate-700 leading-relaxed">
        Je m’appelle <strong>Sami Chabane</strong>, développeur web junior basé à <strong>Lyon</strong>. J’aide les <strong>indépendants, artisans et associations</strong> à gagner en crédibilité en ligne grâce à des sites <strong>simples, beaux et efficaces</strong>.
      </p>
      <p class="mt-4 text-slate-700 leading-relaxed">
        Mon approche : <strong>clarifier votre message</strong>, <strong>mettre en valeur</strong> ce qui vous différencie, et <strong>livrer rapidement</strong> un résultat propre que vous serez fier de montrer.
      </p>
      <p class="mt-4 text-slate-700 leading-relaxed">
        Aujourd’hui en formation <strong>Java & Angular</strong>, je m’appuie sur des bases solides en <strong>Ruby, HTML, CSS</strong> et un peu de <strong>JavaScript</strong>. Avec mon expérience en <strong>community management</strong>, je combine technique et communication pour proposer des sites web à la fois <strong>efficaces</strong> et <strong>impactants</strong>.
      </p>

      <div class="mt-6 flex gap-3">
        <a href="/CV_Sami_Chabane.pdf" target="_blank" rel="noopener noreferrer" class="rounded-xl border px-5 py-3 hover:border-sky-600 hover:text-sky-600 transition">📄 Télécharger mon CV</a>
        <a href="#contact" class="rounded-xl bg-sky-500 px-5 py-3 text-white shadow hover:bg-sky-600 transition">Me contacter</a>
      </div>
    </div>

    <!-- Photo -->
    <div data-aos="zoom-in" class="flex justify-center">
      <div class="relative inline-block group">
        <div class="absolute -inset-1 rounded-full bg-sky-500/20 blur opacity-0 transition duration-300 group-hover:opacity-100" aria-hidden="true"></div>
        <img src="<%= asset_path 'projects/sami.jpg' %>" alt="Sami Chabane"
             class="relative w-56 h-56 sm:w-64 sm:h-64 object-cover rounded-full border-4 border-sky-500 shadow-lg transition duration-300 group-hover:scale-105 group-hover:shadow-2xl group-hover:border-sky-600">
      </div>
    </div>
  </div>
</section>

<section id="contact" class="py-16 md:py-24 bg-white border-t border-slate-200">
  <div class="max-w-3xl mx-auto px-4 sm:px-6">
    <h2 data-aos="fade-up" class="text-2xl md:text-4xl font-bold">Contact</h2>
    <p data-aos="fade-up" data-aos-delay="100" class="mt-4 text-slate-700">
      Un projet, une question, une opportunité d’alternance ? Écrivez-moi :
    </p>

    <% if flash[:_from] == "contact" %>
      <% flash.each do |type, msg| %>
        <% next if type.to_s == "_from" %>
        <% klass = case type.to_s
                    when "notice" then "bg-emerald-50 text-emerald-700"
                    when "alert","error" then "bg-rose-50 text-rose-700"
                    else "bg-slate-100 text-slate-700" end %>
        <div class="mt-4 rounded-xl px-4 py-3 <%= klass %>"><%= msg %></div>
      <% end %>
      <% flash.delete(:_from) %>
    <% end %>

    <%= form_with url: contact_path, method: :post, scope: nil, data: { turbo: false }, local: true, html: { class: "mt-8 grid gap-4" } do |f| %>
      <div class="grid sm:grid-cols-2 gap-4">
        <%= f.text_field :name, placeholder: "Votre nom", required: true, autocomplete: "name", class: "w-full rounded-xl border border-slate-300 px-4 py-3 focus:outline-none focus:ring-2 focus:ring-sky-500" %>
        <%= f.email_field :email, placeholder: "Votre email", required: true, autocomplete: "email", class: "w-full rounded-xl border border-slate-300 px-4 py-3 focus:outline-none focus:ring-2 focus:ring-sky-500" %>
      </div>

      <%= f.text_field :subject, placeholder: "Sujet", class: "w-full rounded-xl border border-slate-300 px-4 py-3 focus:outline-none focus:ring-2 focus:ring-sky-500" %>
      <%= f.text_area :message, rows: 5, required: true, placeholder: "Votre message", class: "w-full rounded-xl border border-slate-300 px-4 py-3 focus:outline-none focus:ring-2 focus:ring-sky-500" %>
      <%= f.text_field :website, value: "", autocomplete: "off", tabindex: -1, class: "hidden" %>

      <%= f.submit "Envoyer", class: "justify-self-center inline-flex items-center gap-2 rounded-xl bg-sky-500 px-5 py-3 text-white shadow hover:bg-sky-600 transition" %>
    <% end %>
  </div>
</section>

<script>
  document.addEventListener("DOMContentLoaded", () => {
    const y = document.getElementById("year");
    if (y) y.textContent = new Date().getFullYear();

    // mini carrousel
    const root = document.querySelector("[data-carousel]");
    if (!root) return;
    const slides = [...root.querySelectorAll(".carousel-slide")];
    const dots   = [...root.querySelectorAll(".carousel-dot")];
    let i = 0, timer;

    const show = (n) => {
      i = (n + slides.length) % slides.length;
      slides.forEach((s, k) => s.classList.toggle("hidden", k !== i));
      dots.forEach((d, k) => d.classList.toggle("bg-slate-900", k === i));
      dots.forEach((d, k) => d.classList.toggle("bg-slate-300", k !== i));
    };

    root.querySelector("[data-action='prev']")?.addEventListener("click", () => show(i-1));
    root.querySelector("[data-action='next']")?.addEventListener("click", () => show(i+1));
    dots.forEach((d, k) => d.addEventListener("click", () => show(k)));

    const autoplay = root.dataset.autoplay === "true";
    const interval = Number(root.dataset.interval || 3500);
    const pauseOnHover = root.dataset.pauseOnHover === "true";

    const start = () => { if (autoplay) timer = setInterval(() => show(i+1), interval); };
    const stop  = () => { if (timer) clearInterval(timer); };

    if (pauseOnHover) {
      root.addEventListener("mouseenter", stop);
      root.addEventListener("mouseleave", start);
    }
    start();
    show(0);
  });
</script>
